name: Deploy to Server

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 14

    - name: Install dependencies
      run: npm install

    - name: Run tests
      run: npm test

    - name: Build Docker image
      run: |
        docker build -t ${{ github.repository }}:${{ github.sha }} .
        docker tag ${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:${{ github.sha }}

    - name: Authenticate to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Push Docker image to GitHub Container Registry
      run: docker push ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:${{ github.sha }}

    # - name: SSH into the server and pull the latest image
    #   uses: appleboy/ssh-action@v0.7.1
    #   with:
    #     host: ${{ secrets.SERVER_HOST }}
    #     username: ${{ secrets.SERVER_USERNAME }}
    #     password: ${{ secrets.SERVER_PASSWORD }}
    #     script: |
    #       docker stop your_container_name || true
    #       docker rm your_container_name || true
    #       docker pull ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:${{ github.sha }}
    #       docker run -d --name your_container_name -p 80:80 ghcr.io/${{ github.repository_owner }}/${{ github.repository }}:${{ github.sha }}
